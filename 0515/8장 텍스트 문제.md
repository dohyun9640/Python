## 중간점검

1-1(370p): 객체 지향 프로그래밍은 객체들을 조합하여서 프로그램을 작성하는 기법이다.

1-2(370p): 가장 큰 단점은 실행 속도가 상대적으로 느리고 초기 설계가 복잡하다는 점이다.

1-3(370p): 서로 관련된 데이터와 그 데이터를 조작하는 기능을 하나의 캡슐 안에 단단히 묶어두는 것을 말한다.

1-4(370p): 특정한 객체를 만들어내기 위해 변수와 메서드를 정의해 놓은 틀이다.

1-5(370p): 클래스라는 설계도를 바탕으로 컴퓨터 메모리상에 실제로 만들어져 동작하는 구체적인 실체를 뜻한다.

1-6(370p): 클래스는 추상적인 데이터 타입이고 인스턴스는 구체화된 실체이다.

2-1(376p): class 키워드를 사용하여 정의합니다.

2-2(376p): 일반적으로 파스칼 케이스(PascalCase) 또는 어퍼 카멜 케이스(UpperCamelCase) 방식으로 작명한다.

2-3(376p): 클래스 이름 뒤에 괄호()를 붙여서 마치 함수를 호출하듯이 생성한다.

2-4(376p): 점(.) 연산자를 사용하여 접근한다.

2-5(376p): 클래스 내부에 정의된 함술를 메서드라고 부른다.

2-6(376p): self는 메서드가 호출될 때 해당 메서드를 실행하고 있는 인스턴스 자기 자신을 가르킨다.

2-7(376p): __init__()은 생성자라고 불리는 특별한 메서드이다.

3-1(383p): 클래스 안의 데이터를 외부에서 마음대로 변경하지 못하게 하는것이다.

3-2(383p): 은닉된 변수에 접근하기 위해 만드는 메서드이다.

3-3(383p): 파이썬에서 변수를 외부에서 직접 접근할 수 없는 비공개 상태로 만들려면 변수 이름 앞에 밑줄 두 개(__)를 붙이면 된다.

4-1(388p): 원본 객체를 가리키는 메모리 주소(참조값)이 전달된다.

4-2(388p): 객체 생성은 컴퓨터의 메모리 공간 어딘가에 [채널 11, 볼륨 10, 전원 켜짐(True)]
이라는 데이터를 가진 실제 텔레비전 인스턴스가 딱 1개 만들어진다. 
새로운 텔레비전 객체가 복사되어 만들어지는 것이 절대 아니다.
텔레비전 객체의 메모리 주소만 똑같이 복사하여 가리키게 된다.

5-1(391p): 인스턴스는 각각의 객체가 개별적으로 소유하고 클래스는 클래스 자체가 소유하며
해당 클래스로 만들어진 모든 인스턴스가 똑같은 값을 공유한다.

5-2(391p): 모든 메서드의 바깥 영역에 변수를 선언하기만 하면 자동으로 클래스 변수가
된다.

6-1(394p): 객체를 사람이 읽기 쉬운 문자열 형태로 표현해야 할 때 자동으로 호출된다.

6-2(394p): 객체 간의 값을 비교하려면 클래스 안에 __eq__(self, other) 메소드를 정의해야 한다. 


## exercise

1(398p): p.data

2(398p): __init__

3(398p): Good Morning

4(398p): 
class Myclass:
    def  __init__(self):
        self.radius = 10

    def set_radius(self, radius):
        self.radius = radius

5(399p): 
class Myclass:
    def __init__(self, r):
        self.radius = r

a = Myclass(10)
print(a.radius)

6(399p): 1, 인스턴스 변수와 클래스 변수의 이름이 같을 경우 인스턴스를 통해 접근하면 인스턴스 변수가 우선적으로 출력됨.

7(399p): Bark!, Animal sound