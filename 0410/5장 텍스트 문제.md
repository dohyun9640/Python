### 5장 텍스트 문제

## 중간점검

1-1(207p): 특정작업을 수행하는 명령어들의 모음에 이름을 붙인것이다.

1-2(207p): 프로그램을 작성하다 보면 동일한 처리를 반복해야 하는 경우가 자주 생기는데 이런 경우에 재활용은 프로그래밍에서 중요한 기술이라서 함수를 사용해야 한다.

2-1(211p): 인수라고 한다.

2-2(211p): 매개변수라고 한다.

2-3(211p):
def get_two_numbers():
    a = int(input("첫 번째 정수: "))
    b = int(input("두 번째 정수: "))
    return a, b

n1, n2 = get_two_numbers()
print(f"입력받은 값: {n1}, {n2}")

3-1(213p):
def get_rect_area(w, h):
    reurn w * h

3-2(213p):
def main():
    result = get_rect_area(10,20)
    print(f"사각형의 면적: {result}")

4-1(220p): 함수를 정의할 때 외부에서 들어올 값을 받기 위해 미리 지어둔 변수의 이름이 "매개변수" 이다.

함수를 호출할 때 매개변수에 실제로 집어넣는 구체적인 값은 "인수"이다.

4-2(220p): 함수를 정의할 때 매개변수에 기본값을 미리 설정해두는 것을 말하는것을 "디폴트 인수"라고 한다.

4-3(220p): 함수를 호출할 때 인수의 값을 단순히 순서대로 넣는 것이 아니라 매개변수의 이름을 직접 지정해서 전달하는 방식을 "키워드 인수" 라고 한다.

4-4(220p): 매개변수 앞에 * 기호가 붙는 것은 "가변 인수"를 의미한다.

5-1(227p): 값을 반환하는 키워드는 "return"이다

5-2(227p):
def addsub(x, y):
    sum_val = x + y
    sub_val = x - y
    return sum_val, sub_val

result_add, result_sub = addsub(10, 5)

print(f"더한 값: {result_add}")
print(f"뺀 값: {result_sub}")

6-1(230p): 코드의 재사용성, 가독성 향상, 유지보수의 용이성, 모듈화 및 협업과 같은 장점이 있다.

6-2(230p): 구조화 프로그래밍은 3개로 나눌 수 있다.
    순차 구조: 명령어를 위에서 아래로 순서대로 실행한다.
    
    선택 구조:if-else와 같은 조건문을 통해 상황에 따라 실행 경로를 나눈다.

    반복 구조:for, while과 같은 반복문을 통해 작업을 되풀이 한다.

7-1(236p): 순환 호출이란 함수 내부에서 자기 자신을 다시 호출하는 것을 말한다.

7-2(236p): 
"1부터 n까지의 합은 n에다가 (1부터 n-1까지의 합)을 더한 것과 같다."

def sum_recursive(n):
    if n == 1:
        return 1
    else:
     (n-1까지의 합)
        return n + sum_recursive(n - 1)

num = int(input("어디까지 더할까요?: "))
print(f"1부터 {num}까지의 합은 {sum_recursive(num)}입니다.")

8-1(240p): 지역 변수는 함수 내부에서 선언되서 설정된 변수이고 전역변수는 함수 외부 즉 전체에서 선언된 변수이다.

8-2(240p): 함수 내부에서 전역 변수의 값을 읽는 것은 언제든 가능하다.

8-3(240p): 함수 안의 지역 변수 값만 바뀌고 함수 밖의 전역 변수 값은 그대로 유지된다.


## 도전문제

1-1(221p):
def display(msg="Welcome", count=1):
    for k in range(count):
        print(msg)

display(count=5)

2-1(221p):
display(msg="반갑습니다", count=3)

3-1(224p):
def weeklyPay(hours, rate=10000):
    money = 0
    if (hours > 30):
        money = rate * 30 + 1.5 * rate * (hours - 30)
    else:
        money = rate * hours
    return money


3-2(224p):
def weeklyPay(hours, rate=10000):
    money = 0
    if (hours > 30):
        money = rate * 30 + 1.5 * rate * (hours - 30)
    else:
        money = rate * hours
    return money

r = int(input("시급을 입력하세요: "))
h = int(input("근무 시간을 입력하세요: "))
result = weeklyPay(hours=h, rate=r)
print(f"주급은 {result}원입니다.")

4-1(227p):
def sort_numbers(n1, n2):
    if n1 > n2:
        return n2, n1
    else:
        return n1, n2

num1 = int(input("첫 번째 정수를 입력하세요: "))
num2 = int(input("두 번째 정수를 입력하세요: "))
low, high = sort_numbers(num1, num2)
print(f"결과: 작은 값은 {low}, 큰 값은 {high}입니다.")

## exercise

1-1(244p): max (a,b)

2-1(224p): 10

3-1(244p): c

4-1(244p): 함수 안에서 매개변수로 받은 값을 수정하더라도 그 변수가 숫자나 문자열 같은 불변객체라면 함수 외부의 원본 변수 값은 변하지 않는다.

5-1(245p): 함수 내부에서 global x를 선언하면 함수 안에서 x를 수정할 때 새로운 지역 변수를 만드는 대신 함수 밖의 전역 변수 x를 직접 수정하게 된다.

6-1(245p): 사용자가 0 또는 양수를 입력할 경우, if x < 0: 이 거짓이 되어 변수 y가 생성되지 않는다.

7-1(245p): sub() -> 2 3 sub(a=10) -> 10 3 sub(5,6) -> 5 6 sub(b=10) -> 2 10

8-1(245p): 실행결과: 30 -10

이유: 파이썬 함수에서 return a+b, a-b처럼 여러 값을 쉽표로 구분해서 반환하면 튜플 형태가 된다. x,y = (30, -10)과 같이 작성하면 각각의 변수에 값이 순서대로 할당된다.

9-1(245p): 마지막 줄의 print(local)에서 오류가 발생한다.

이유: local 변수는 sub()함수 내부에서 정의된 지역 변수이다. 함수 외부에서는 접근할 수 없기 때문에 파이썬은 local 이라는 변수를 찾지 못해 오류를 낸다.